# V2.1-LICK 独立候选构建基线

## 固定身份

- 版本：`2.1-LICK`（Windows 文件版本 `2.1.1.0`）。
- 基础标签：`BASE-001`。
- 启用功能：`idle-random-left-right-hand-lick`（左右空闲随机舔手）。
- 测试版：`true`。
- 调试菜单：`false`。
- 文档基线：本文件 `V2.1_LICK_BUILD.md`。
- 唯一候选名：`桌面宠物_空闲随机舔手.exe`。

## 每次构建写入的信息

`build_idle_lick_candidate.ps1` 在构建时生成并打包 `build-info.json`，其中写入上述固定身份、UTC 构建日期和当次 `git rev-parse --short HEAD`。日期与短哈希故意在构建时确定，不能由源码提交前的占位值冒充。

## 范围与门禁

候选继承 BASE-001 的已认可猫头、猫身、眼睛、逐像素 Alpha、眼球跟随、眨眼、头颈连续形变和 minimum-jerk；本增量不修改素材和转头角度。新增逻辑只有空闲左右随机舔手状态机及其仲裁，不包含耳朵、双前肢、饥饿、拖放或喂食。

Windows 构建必须先跑完整测试，再生成唯一 EXE，检查 50 MiB 上限并输出 SHA-256。Actions 结果、下载后哈希核对、真实 Windows 桌面运行和用户视觉验收是互相独立的门；仓库内 Linux 自动检查不能代替后三项。
