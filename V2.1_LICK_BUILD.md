# V2.1-LICK 独立候选构建基线

## 固定身份

- 版本：`2.1-LICK`（Windows 文件版本 `2.1.1.0`）。
- 基础标签：`BASE-001`。
- 启用功能：`idle-random-left-right-hand-lick`（左右空闲随机舔手）。
- 候选性质：`未自动测试候选版`。
- 自动测试：`false`（用户已决定旧基线自动测试不阻塞本 EXE）。
- 验收状态：`awaiting-user-windows-validation`。
- 调试菜单：`false`。
- 文档基线：本文件 `V2.1_LICK_BUILD.md`。
- 预定候选名：`桌面宠物_空闲舔手返工候选.exe`。
- 当前发布状态：`blocked-foundation-and-reviewed-assets`。

## 每次构建写入的信息

`build_idle_lick_candidate.ps1` 在构建时生成并打包 `build-info.json`，其中写入上述固定身份、UTC 构建日期和当次 `git rev-parse --short HEAD`。日期与短哈希故意在构建时确定，不能由源码提交前的占位值冒充。

## 范围与门禁

候选继承 BASE-001 的已认可猫头、猫身、眼睛、逐像素 Alpha、眼球跟随、眨眼、头颈连续形变和 minimum-jerk；本增量不修改素材和转头角度。新增逻辑只有空闲左右随机舔手状态机及其仲裁，不包含耳朵、双前肢、饥饿、拖放或喂食。

已移除被拒收的固定坐标几何前肢/舌头。替代合成接口只接受审核后的原猫左右前肢、原位补洞、嘴舌、遮罩和锚点包，并要求调用者传入当前头部锚点映射；缺包时不生成占位图。QA 接触表只能在真实包到位后生成到临时目录，不提交 PNG。

Windows Actions 保留不运行 pytest 的仅打包路线，但构建脚本先检查 PR5 `ActivityCoordinator` 和审核素材 manifest。当前两项均缺失，因此 workflow 应失败且不得上传 artifact。依赖补齐并完成真实 `main -> window/input -> coordinator -> adapter -> asset compositor -> renderer` 接线后，才执行 PyInstaller、唯一文件名、大小与 SHA-256 检查并上传等待用户实机验收的候选。
