# V2.1-LICK 独立候选构建基线

## 固定身份

- 版本：`2.1-LICK`（Windows 文件版本 `2.1.1.0`）。
- 基础标签：`BASE-001`。
- 启用功能：`approved-cat-left-paw-groom-frames`（仅用户认可的猫自身左前爪候选；未镜像另一侧）。
- 候选性质：`未自动测试候选版`。
- 自动测试：`false`（用户已决定旧基线自动测试不阻塞本 EXE）。
- 验收状态：`awaiting-user-windows-validation`。
- 调试菜单：本独立验收候选的 `test_build=true`、`debug_menu=true`，右键“调试 → 左前爪舔手（3次）”立即请求现有动画；生产版或缺少明确测试元数据时默认关闭。
- 文档基线：本文件 `V2.1_LICK_BUILD.md`。
- 预定候选名：`桌面宠物_舔手逐帧动画恢复.exe`。
- 当前发布状态：`ready-for-windows-build-pending-validation`。

## 每次构建写入的信息

`build_idle_lick_candidate.ps1` 在构建时生成并打包 `build-info.json`，其中写入上述固定身份、UTC 构建日期和PR 事件的 source HEAD（workflow_dispatch 则为 `github.sha`）。日期与短哈希故意在构建时确定，不能由源码提交前的占位值冒充。

## 范围与门禁

候选继承 BASE-001 的已认可猫头、猫身、眼睛、逐像素 Alpha、眼球跟随、眨眼、头颈连续形变和 minimum-jerk；本增量不修改素材和转头角度。新增内容仅为用户认可的单侧整帧舔手序列及其现有运行所有权接线，不包含耳朵、双前肢、饥饿、拖放或喂食。

已移除被拒收的固定坐标几何前肢/舌头。唯一审核图源以每行不超过 120 字符的 Base64 文本保存；构建时解码并核验原 PNG SHA，再确定性生成 12 帧。运行帧只进入临时构建目录和 EXE，不提交 PNG、GIF 或联系表。

当前分支没有 `RuntimeContext` 或 `ActivityCoordinator` 实现；不得将 `GroomFramePlayer` 冒充公共基础。候选沿用现有 `RuntimeEyeSession` 的唯一 tick、`following/playing` 所有权和输入中断路径，未另建全局状态机；本分支也没有饥饿/健康服务可供接线。构建脚本检查该现有仲裁入口及审核素材 manifest，再执行确定性解码、逐帧重建、PyInstaller、唯一文件名、大小与 SHA-256 检查。

Windows CI 在临时 `APPDATA`、`LOCALAPPDATA`、`TEMP`、`TMP` 和独立实例锁环境中启动候选，跟踪本次 PyInstaller 父子进程，七秒后要求其进程树内存在名称为“桌面宠物 V2.1-LICK 调试候选”的可见 `TkTopLevel` 窗口，启动错误弹窗视为失败。最后仅终止本次自建进程树并清理临时目录。该检查不能代替动作、透明边缘和多屏行为的实机验收。

已知视觉边界：整帧播放目前直接切换当前跟随姿态与固定默认端点；非中性头部姿态可能在进入/退出舔手时发生跳变。现有 `pause_and_recenter` 会暂停提供舔手采样的唯一时钟并转入 `playing`，不能直接套用，否则可能冻结；需要独立完成播放生命周期协调后再消除此风险。本次调试入口修复未改动已验收头部跟随曲线或认可动作素材。

## Windows run 34304052388 返修记录

- Windows 构建成功；取回 artifact 中 EXE 的实际 SHA-256 为 `712bdfbbf38ab9adabe4899a414f66c5cdebdf5eaf1b28012ed6e8eaf1ab4be5ff`。
- 该 artifact **视觉拒收**：解包的旧 `runtime/04.png` 主体被格边缘碎片扩大裁框后缩小，`y≈730` 还残留两块相邻帧脚部碎片；不得把构建成功写成视觉通过。
- 根因是旧导入器对整张 sheet Alpha 的总包围框裁切，而不是先选定每格主体连通域。现导入器先取每格最大不透明连通域、保留其邻近抗锯齿，再等比例缩放和脚底锚定；不与 canonical 叠加。
- 旧 build-info 的 `93839a6` 是 Actions PR merge checkout，而 PR12 源 HEAD 是 `41cbfe8`。Workflow 现在显式传入 PR source SHA；build-info 不再把 merge commit 写成源提交。
- 本地修复后的黑/白底 12 帧预览已检查；自动证据证明主体连通域落地且高度一致，但 Windows 新 artifact 和用户视觉复核仍待总控执行，因此状态仍不是视觉通过。
