# Desktop Pet V2.1 Final Baseline Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use test-driven-development, systematic-debugging, Ponytail, verification-before-completion, and the applicable project desktop-pet Skills. Steps use checkbox syntax for tracking.

**Goal:** Close BASE-001 on the existing approved head-neck candidate without creating features or changing art.

**Architecture:** Keep the current motion rig and packaging path. Change only the randomized tilt envelope, release metadata/contracts, reproducible test dependencies, Windows artifact gate, Skills, and baseline record; retain the independent 50-degree pose/mesh safety ceilings.

**Tech Stack:** Python 3.11+, Pillow 11, pytest 8, PyInstaller 6, PowerShell, GitHub Actions.

## Global Constraints

- Actual left/right random tilt range is 18–30 degrees.
- Existing approved images and old EXEs are immutable.
- Minimum-jerk, eye follow, blink, Alpha, and continuous head-neck deformation remain enabled.
- No V2.1 ear, forelimb, hand-licking, hunger, drag/drop, or feeding feature is implemented.
- Windows Actions and real user desktop acceptance are distinct from Linux checks.

## Tasks

- [x] Add and observe failing tests for left minimum/maximum, right minimum/maximum, minimum-jerk, packaging, version, dependencies, and workflow gates.
- [x] Apply the minimal motion and release configuration changes without changing mesh safety boundaries or assets.
- [x] Add the four missing project Skills and repository controller instructions.
- [x] Run focused and full tests, compile checks, asset hashes, and diff checks; record dependency/display and pre-existing golden-baseline failures honestly.
- [ ] Record the final commit and pending Windows/user gates, commit, and create the PR.
