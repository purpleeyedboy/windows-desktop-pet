# V2.1 Drag Repair Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use test-driven-development and verification-before-completion. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the rejected format-only drag preview with a foundation-compatible adapter and truthful blocked delivery state until PR5 services are available.

**Architecture:** Define contracts only for the shared PR5 services; do not implement a second coordinator, clock, health model, input queue, or store. Parse and release native `CF_HDROP` synchronously, copy only one path into immutable values, and pass versioned validation/drop events through the injected shared services. Runtime registration is conditional on a fully supplied adapter, so the rejected legacy controller cannot masquerade as repaired behavior.

**Tech Stack:** Python 3.11, ctypes Win32 OLE, dataclasses/Protocols, Pillow/Tk integration, PyInstaller.

## Global Constraints

- Approved cat source assets and existing head follow/blink code are immutable.
- No file content read, move, copy, delete, recycle, open, or upload operation.
- No independent AppState, event queue, Clock, StateStore, HungerMeter, or FileValidationService implementation.
- No acceptance EXE until the shared foundation commit and services are injected and real Windows checks are complete.

## Tasks

- [x] Add red tests for immutable single-path OLE payloads, multi/folder/non-local rejection and Drop event value copying. Native `STGMEDIUM` release still requires Windows evidence.
- [x] Add red tests for versioned validation, stale callback rejection, serial InputRouter dispatch, head-only Copy, full-health None, and cancel/recovery.
- [x] Implement the foundation service Protocols and adapter without owning shared state or time.
- [x] Replace format-only OLE handling with payload extraction and adapter delegation; wire `PetWindow` only when the foundation adapter is supplied.
- [x] Replace fixed 116%/rectangle/Alpha-clipped composition with 120% local eyes, Alpha-derived contour and independent particles. Coordinator recovery remains pending PR5.
- [x] Move drag demo commands into one Debug submenu and add visible runtime/build identity.
- [ ] After PR5 foundation is available, integrate the concrete services in `main`, verify entry-to-unregister reachability, then enable the repaired candidate build.

## 2026-09-06 second-round integration attempt

Blocked before mutation: the requested PR5 commit is absent locally and every GitHub transport available to this environment returned 403/401. Evidence is recorded in `qa/drag-expectation/pr5-fetch-blocked-20260906.md`. The speculative local Protocols were not treated as the authoritative PR5 API, and no claim of `create_application_services` integration was made.
