# 耳朵防触摸系统 — 2026-09-10

## Current increment

- Supersedes the old three-shake / throw choreography and 500 ms cooldown.
- Each released click plays exactly one 1000 ms sequence. Peak recoil is reached
  after 160 ms, followed by a single return to the exact neutral image.
- While active, another click is ignored, not queued or auto-repeated. Once the
  action completes, the next click is immediately accepted on either ear.
- Preserves release hit testing, drag discrimination, shared activity arbitration,
  interruption recovery and stale callback protection.
- New EXE identity: `桌面宠物_耳朵防触摸系统_单次躲闪-20260910.exe`.

## Evidence and boundaries

The new behavior tests first failed against the original 550 ms / cooldown code,
then passed. The runtime wiring verifier exercises the actual shared queue,
compositor, recovery and 100 alternating requests, including immediate replay.

This increment reorders existing lossless ear poses; it does **not** claim new
full-cat artwork or visually approved backward ear motion. The uploaded reference
video was absent from this workspace. Recovery located its 88,512-byte MP4, but
the transfer returned HTTP 502, so its timing/content was not inspected.
The full-body contact sheet was inspected: body identity is stable, but the ear
recoil remains restrained. Actual Windows interaction and video matching remain
pending; no Windows EXE was locally built on Linux.

The existing metadata test still expects the old `e178f371` foundation, whereas
the source already identifies `4eda8964...`. The existing packaging test expects
an obsolete short-hash command. Those unrelated stale assertions were not
weakened to claim a full-suite pass. The legacy ear test also imports removed
procedural APIs. Current focused tests target the real raster adapter.

Reproduce previews with `tools/build_ears_preview.py --output <png> --animation <gif>`.
Approved artwork and the original embedded source-pose JSON remain unchanged.

## Follow-up: release contract repair

The earlier stale packaging assertions now match the validated 40-character
source SHA, seven-character prefix, and current `4eda8964...` foundation.
The historical 29-file manifest keeps its original `e178f371...` provenance.
All 158 baseline asset hashes remain locked; only the already-added text
`assets/keyframes/playback.json` is allowed as an additional asset. Preview
dimensions follow the actual eight-frame recoil instead of the old 18 frames.
Headless window tests record raster keyframe calls and exact neutral restoration
instead of removed angle APIs.

Evidence: 11 focused release/recoil tests and two headless ear lifecycle tests
pass; runtime wiring, shared recovery and graphic playback verifier scripts pass.
Native Tk tests cannot run without a display. The obsolete procedural API suite
`test_ear_interaction.py` is not included; this is not a full-suite pass.
The contact sheet was inspected: body identity is stable, recoil still restrained.
Reference video matching and Windows visual acceptance remain pending.
No artwork changed.
