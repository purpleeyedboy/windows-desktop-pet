# V2.1-HUNGER 饥饿值与饥饿动画

- 版本：2.1.1 测试版；日期：2026-09-04。
- 基础标签：`BASE-001`；文档基线：V2.1。
- 启用功能：BASE-001 全部能力、整数饥饿值、UTC 离线恢复、分级饥饿与流泪气泡动画、原子存储、幂等奖励窄接口。
- 调试菜单/时间模拟：仅测试构建允许注入可变时钟；生产配置硬拒绝。
- 明确排除：耳朵、前肢、舔手、拖放、文件喂食以及任何文件接收、删除、回收。
- 构建时 `build_metadata.json` 写入 Git 短哈希；Windows Actions 校验唯一 EXE、测试、50 MiB 大小上限和 SHA-256。

状态文件使用版本 1 JSON，位于用户本地应用数据目录；测试全部使用 pytest 临时目录，不读取真实用户状态。
