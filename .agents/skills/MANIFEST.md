# Repository-scoped Codex skills

These skills are committed with the project so a cloud task does not depend on the local machine's personal `C:\Users\rog\.codex\skills` directory.

## Required for continuation

- `ponytail`: scope control, truth mode, minimal verified changes, and shipping discipline. Invoke as `$ponytail` for all subsequent work.
- `subagent-driven-development`: resume the durable R1-R5 task ledger with a fresh implementer and an independent reviewer for each task.
- `systematic-debugging`: required before another asset-generation or rendering fix attempt.
- `test-driven-development`: required for code or behavior changes.
- `requesting-code-review` and `verification-before-completion`: final review and evidence gates.
- `writing-plans`, `using-git-worktrees`, `finishing-a-development-branch`, and `using-superpowers`: plan/branch lifecycle dependencies.

The platform-provided `imagegen` skill is intentionally not copied. Use the current system-provided version and built-in image-generation tool in the cloud environment.

The copied `ponytail` payload contains 11 files and excludes its source repository's nested `.git` directory.
