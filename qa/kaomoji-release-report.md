# 六帧猫耳颜文字版发布核验报告

日期：2026-08-28

## 已通过

- 动作素材：3 个动作、每个 6 帧，共 18 张；全部为 512×768 RGBA。
- 台词库：jump、squash、shake 各 180 条中文 + 20 条颜文字，共 600 条全局唯一台词。
- 字体与排版：中文 28px、颜文字 40px；ZCOOL、Noto Sans Variable、Noto Sans Math 均为内置资源，不使用系统字体回退。
- 自动测试：139 个核心测试 + 5 个逐像素透明窗口测试 + 23 个桌宠交互测试，共 167 个通过，0 个失败。
- PyInstaller：成功生成唯一单文件 EXE；归档核对通过 18 张关键帧、5 张气泡图、3 个字体、3 份许可、1 个台词库和 1 份第三方 notice，归档字节与源文件一致。
- EXE 启动：首个实例持续运行；第二个实例在 5 秒内以退出码 0 退出，单实例锁有效。
- 运行时字体探针：`_MEI493682/assets/fonts/` 中三套字体均存在；ZCOOL 1,514,968 字节、Noto Sans Variable 2,049,096 字节、Noto Sans Math 1,015,396 字节。
- 交付目录：EXE、600 句 JSON、第三方 notice 和三份 OFL 已复制完成。

## 交付哈希

- `桌面宠物-6帧猫耳颜文字版.exe`
  - 字节数：30,822,553
  - SHA-256：`594EE6FD95BEA7B681551196F448FB3DD62B9CC49BECAE19DB3A0477776842B0`
- `台词库-600句.json`
  - 字节数：16,964
  - SHA-256：`829179B422184A92FAA9D164F839D7B8AB0233B08F4E24904961B43EA4CE4CDB`

## 证据边界与待完成的人工桌面 QA

以下项目必须由用户在可见 Windows 桌面操作，进程内 Tk 测试、自动点击或静态图片不能替代：

1. 连续有效点击角色 9 次，确认动作顺序为 `jump, squash, shake` 循环三轮；每次只出现当前动作库中的一条随机台词，同一动作相邻两次不重复。
2. 分别把角色拖到工作区上、下、左、右边缘并触发气泡，确认气泡不遮挡角色、不超屏，尾巴始终指向角色。
3. 保存真实桌面截图为 `qa/cat-ear-bubble-runtime.png`。

截图只证明截图时刻的视觉状态，不证明随机抽取概率或长期无重复性。

## 环境说明

首次构建在 Codex 文件沙箱中因 `%TEMP%` 写入与清理权限被拒绝而中止；同一未修改构建脚本在获准的 Windows 环境中重跑后全部通过。该失败属于执行环境权限限制，不是程序测试失败。

## 最终独立审查

整分支审查未发现 Critical 或 Important。以下 Minor 不阻塞本次发布，保留为后续维护项：

- 缺字失败测试锁定了上下文和缺失码位，尚未把完整台词文本写入断言。
- 台词迁移工具的已迁移、未知哈希、生成哈希和写后分支尚无直接单元测试；最终生产 JSON 已通过哈希、数量、唯一性和逐句渲染验证。
- `bubble_layout.py` 的两个最小宽度常量尚未被消费，校验模块保留了相同的数值常量。
- 两份固定上游 Noto OFL 原文包含行尾空格，导致整分支 `git diff --check` 对许可文件报告空格；为保持许可文件与固定来源字节及 SHA-256 一致，本次不改写其内容。
