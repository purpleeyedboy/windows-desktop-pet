# Jump 30 帧 AI 补帧 QA 报告

状态：`PASS`

## 生成与组装

- 五个关键帧区间分别使用内置 imagegen 生成 `3×2`、纯 `#0000FF` 蓝幕过渡姿势网格。
- 各区间使用的中间帧数为 `5 / 5 / 4 / 5 / 5`，共 24 张 AI 过渡帧。
- 最终关键帧位置为 `00 / 06 / 12 / 17 / 23 / 29`。
- `tools.process_ai_transitions` 完成蓝幕抠除、与透明背景连通的蓝边去色、按端点 bbox 平滑归位和 QA 输出。

## 文件合同

- `assets/pet/jump` 恰好包含 `00.png–29.png` 共 30 帧。
- 30/30 均为 `512×768 RGBA`。
- 透明像素中的非零 RGB：`0`。
- 画布四周非透明像素：`0`。
- 六张归档关键帧与输出位置逐字节一致：`6/6`。
- 全树验证：`OK: 3 actions, 90 frames, 512x768 RGBA`。

## 视觉检查

- 已逐段查看 5 行高清接触表，30 帧均为单一猫体。
- 未发现双头、双眼、重复耳朵、重复四肢、双轮廓、半透明鬼影或光流拉丝。
- 动作顺序连续呈现：直立准备 → 下蹲 → 离地前伸 → 空中收腿 → 落地压低 → 回到直立。
- 第二轮去蓝后，24 张 AI 过渡帧在 2 像素轮廓带内的明显蓝色占优像素均为 `0`；原关键帧未做任何改写。

## QA 产物

- `qa/jump/contact-sheet.png`
- `qa/jump/normal.gif`
- `qa/jump/slow.gif`
- `qa/jump/stats.json`
- `qa/ai_sources/jump/manifest.json`
