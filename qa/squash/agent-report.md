# Squash 30 帧 AI 补帧 QA 报告

状态：`PASS`

- 五段 AI 网格提供 `5 / 5 / 4 / 5 / 5` 张过渡姿势，共 24 张。
- 30 帧按 `00 / 06 / 12 / 17 / 23 / 29` 插入六张原关键帧；关键帧逐字节一致。
- 逐段高清接触表确认动作连续：直立 → 逐级变宽变矮 → 最低压扁点 → 逐级拉高变窄 → 回弹落稳 → 恢复。
- 首轮发现相邻网格泄漏的悬空小爪；加入“整图提取完整主体 + 每帧只保留最大主体分量”后重新组装，碎片全部消失。
- 未发现双头、双眼、重复四肢、鬼影、双轮廓或光流拉丝。
- 24 张 AI 帧的明显蓝色轮廓像素为 `0`；透明 RGB 和画布边界均为 `0`。
- QA：`contact-sheet.png`、`normal.gif`、`slow.gif`、`stats.json`。
